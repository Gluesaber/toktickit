# dependencies
node_modules/
# env & secrets
.env
*.env
!.env.example
# build output
dist/
build/
# prisma
server/prisma/*.db
# uploaded attachments (Issue 2-7) — stored on local disk in dev, never committed
server/uploads/*
!server/uploads/.gitkeep
# logs & OS
*.log
.DS_Store
# Playwright (Issue 2-8) — local run artifacts, not the screenshots we deliberately keep
# (those live under artifacts/lab-02/screenshots/, which is NOT ignored)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
