# dependencies
node_modules/
# env & secrets
.env
*.env
!.env.example
# build output
dist/
build/
# prisma
server/prisma/*.db
# uploaded attachments (Issue 2-7) — stored on local disk in dev, never committed
server/uploads/*
!server/uploads/.gitkeep
# logs & OS
*.log
.DS_Store
