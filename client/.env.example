# Base URL of the TokTickIT API. Leave empty in dev — vite.config.ts proxies /api to the backend
# same-origin, which the session cookie (BR-09) needs. Copy this file to ".env" for local dev.
VITE_API_URL=""
